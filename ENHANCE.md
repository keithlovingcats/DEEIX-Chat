# ENHANCE

`jun-modified` 分支相对 `dev` 的定制增强功能清单。面向开发与运维，按功能域组织，标注引入提交便于追溯。定制分支定期合并 dev 并将定制功能迁移至新架构，本文件随功能演进同步更新。

- 分支基线：`dev`（main 0.4.0 + 后续提交）
- 净差异规模：477 文件，约 +20786 / -1915 行
- 定制提交：40 个（38 个功能提交 + 2 次 merge dev 架构迁移）

---

## 一、多模型对话体系（核心定制主线）

### 1. 多模型并行对话（`0e7deb38`）

一次提问多模型同时回答，主模型答案落地后其余模型以 `branchReason=retry` 并行 fan-out，每模型独立 run（计费/重连/审计天然隔离）。

- 组合持久化到会话（`parallelModels`），刷新/切换会话后恢复
- 消息树分支标签页（`ModelBranchTabs`）：同模型多次重试归并为一个 tab，tab 内切换版本
- 后端计费预留槽位扩至 20（`UsageReservationMaxActivePerUser`），与前端并发上限对齐并加单槽保底
- 前端编排：`use-chat-message-submit` 的 `overridePlatformModelName` 覆盖链（fan-out / 讨论 / 原模型重试三类请求按目标模型发送）

### 2. 多模型讨论（`ba1e8fc5` → `2b4772f4` → `34bb345a`）

2-20 个模型 N 轮辩论 + 终稿合成：第 1 轮全员盲测（透传原始问题、互不可见，消除首模型锚定），后续轮只审阅此前轮次快照、轮内互不可见且并行（消除轮内序列特权），主模型（或接替者）凭全量 transcript 合成终稿。

- 后端：`chat_messages.discussion_meta_json` 落库与双向透传；发言 prompt 进生成上下文的两级配合——`reuseUserMessage` 分支守卫（不回填原 user content）+ `OverrideReusedUserContent` 以 content 替换链尾 user 文本（不追加，避免与原问题重复），均有单测锁定
- 前端：`use-chat-discussion` 轮间 barrier、轮内 `Promise.all` 并行（每发言独立 run，峰值并发与 `MAX_CONCURRENT_RUNS` 对齐）；聚合气泡 + 讨论过程面板（按模型/全部轮次视图、单卡折叠）；中途停止、单发言失败容错与断线续传去误报
- 终稿容错：总结失败先同模型重试 1 次，仍失败按参与序轮换接替（总尝试上限 5 次），失败尝试同 index 落库可见
- 开关与轮数（1-5 轮，默认 2）为会话内内存态；讨论组不走分支标签页，刷新后凭 meta 重建面板

### 3. 模型级启用/禁用（`5bfa9a34` → `b9fd6b9c` → `da714ad2`）

- 顶栏模型 pill 的 Ban 开关：禁用 = 本轮临时退出对话，组合保留全量（禁用≠删除）
- 禁用语义贯穿发送链：fan-out、入队快照、讨论参与者与讨论门槛（≥2 启用）均只消费启用列表
- 主模型禁用走顺延晋升：首个启用的附加模型接任主模型，原主模型降级为附加并置禁用态；全禁用死锁保护（至少保留一个启用模型）
- 切换会话/新建会话恢复全启用；移除模型/切换主模型时联动收缩禁用集合

### 4. 展示序与分支稳定性（`a2ce7f6b`）

- 多模型兄弟展示序跟随顶部选择器顺序，刷新后同序恢复
- 分支兜底选中按 `createdAt` 取最新，避免模型偏好重排后默认分支漂移到旧回答
- 流式/等待中的 pending exchange 不被 reload 误清理；路由解析失败时 run 模型名以请求名兜底

---

## 二、消息物理删除（`ec44c77d` + `1dfd15ca`）

`DELETE /api/v1/messages/{id}`，user 提问与 assistant 回复均可删：

- 子树级联：删除一条回复连带其下追问链；删除提问连带其下全部回复（多模型兄弟同删）；同轮其他模型回复/重试兄弟不受影响
- 物理删除范围：`chat_messages` 子树 + 附件 + 反馈 + RAG 向量分片（SQLite 方言先清 sqlitevec 虚表）+ 轨迹事件 + 上下文证据；保留 `chat_runs`（计费审计）与审核事件（合规）
- 并发安全：删除事务内校验子树 pending（追问下仍在生成的回复中止并返回 409）；会话 `message_count` 递减带零值保底
- 前端：操作行删除按钮（生成中禁用、请求中转圈）；确认对话框按角色区分文案并显示级联条数；错误提示接入错误码本地化
- 典型场景：删光一轮全部回复后悬空的 user 提问可再删，消除后续上下文中连续两条 user 的形态

---

## 三、全服聊天室（global chat）

实时聊天室子系统（入口「聊天室」，路由 `/global-chat`）。

### 后端（`f9482555`）

- `global_chat_messages` 表：用户信息快照 + text/image 两类消息
- ChatHub 进程内广播：订阅缓冲 128、慢消费者断连自愈、在线去重计数、连接上限 5000
- NDJSON 长连接 `/global-chat/stream`：先订阅后回放原子衔接、30s 心跳、回放超限下发 resync
- 游标分页（自增 id 兼作 seq）、发送返回完整消息供乐观更新、管理员软删除
- 图片复用 `/files` 上传（purpose=global-chat）+ 共享内容端点（不按 owner 过滤，发送侧校验归属与 MIME）

### 前端（`31bed7cf`）

- 消息列表：日期分组、向上加载、吸底、未读浮窗
- `use-global-chat-stream`：指数退避重连 + after_id 断线补全
- 三来源去重合并（实时流/回放/乐观更新）+ resync 整体重拉
- 图片 blob 加载缓存；lightbox 基于 Dialog 的可访问性实现

### 稳定性修复

`6f153182` StrictMode 连接泄漏；`eff59d39` 状态机补 open 态、图片缓存 LRU 上限；`975138c6` 断线补发删除事件、hasMore 预取精确判定；`06d7af02` 乐观气泡排序、翻页未读核销；`fed1e178` 图片解码失败驱逐缓存重试

### 功能增量

`a663b57e` 管理员多选批量删除；`d89faa27` 同账号多设备「其他设备」标记；`157f2f8e` 聊天室命名、设备指纹分边（自己右侧/他人左侧）、头像按 userId FNV-1a 哈希 8 色板着色（明暗适配）

---

## 四、笔记模块（`0e7deb38` → `2b4772f4` → `50f944fd` → `a1f2c2c6`）

- 前后端全栈 CRUD、搜索排序、Markdown 编辑、清单进度统计、编辑器自动保存
- Monaco Markdown 编辑器：换行切换、字符行数统计、快捷清单插入
- 预览优化：链接直接新标签页打开、保留段内换行、弹框加宽

---

## 五、主题与品牌定制

### 代码高亮与 Mermaid（`0e7deb38` + `a2ce7f6b`）

- 设置页可选 60+ shiki 主题与 Mermaid 配色，带预览
- 代码块画布背景跟随所选 shiki 主题（`--sdm-bg`/`--shiki-dark-bg` 双槽，深浅即切）
- Artifact 面板支持任意语言代码阅读视图与按扩展名下载

### 品牌标识

- `0e7deb38`：默认品牌更名 Jun's Chat（logo、文案、关于页）；发送键改 Ctrl+Enter；初始管理员凭据独立打印；屏蔽 Gin 路由注册刷屏
- `90940118` + 3 次 fix：浏览器与 PWA 图标更新为深蓝底白 J 品牌标识（钩形设计，避免误读为 D）

---

## 六、聊天体验细节

| 提交 | 内容 |
| --- | --- |
| `2b4772f4` | 折叠展开视口防跳、输入框拖拽调高与双击复位、生成中重试拦截守卫、思考态优化 |
| `5b01ebcb` | 长消息返回顶部悬浮按钮改 sticky 定位，阅读中途可达 |
| `9d63d14f` | 抽取 IME 组合态守卫 hook，聊天与聊天室输入框复用 |
| `1dfd15ca` | 断线恢复：settledRunIDsRef 排除已终态 run 的瞬态 pending 防重复 resume；message_created 回放按兄弟计数静音 fan-out 误报；代码块折叠文案缺失修复 |
| `ad9db5c4` | 上游失败摘要透传前端：响应钉稳定错误码避免 slug 化，错误码本地化接入，讨论面板/多模型栏直接展示上游失败原因 |

---

## 七、管理后台模型运维（`9cd3b7d4`）

- 孤儿模型清理：模型目录工具栏「清理无上游模型」按钮，徽标实时显示本页无来源（`sourceCount<=0`）模型数——从 `items` 派生零额外请求；点击时全量扫描孤儿并弹确认框（预览前 8 个 + `runBulkActionInChunks` 分批批删，100/批）
- 上游模型批量测试：上游模型对话框多选批量探测，按上游模型名 `/` 前缀（提供商）分组——组间并行（并发 4）、组内串行防同提供商限流（`runSettledGroupedTasksWithConcurrency`），结果保序聚合进探测对话框；未保存/dirty 行自动跳过并 toast 提示
- 探测对话框多结果索引列表：test-all / 批量测试场景在头部与详情间常驻结果列表（状态点/上游模型名/状态码/延迟），点击直达详情、自动滚动跟随；对话框固定高度防结果切换跳动
- 批量操作样式区分：`TableToolbar` 的 bulkActions 支持 `variant: default | destructive`，非删除类批量操作不再一律红色（未传时按 key 含 `delete` 兜底兼容存量调用点）

---

## 八、构建与部署（受限网络适配）

| 提交 | 内容 |
| --- | --- |
| `fcd61aab` | Dockerfile 支持 `NPM_REGISTRY` 构建参数，修复 corepack 下载 pnpm 超时 |
| `f33d3d61` | Dockerfile 支持 `GOPROXY` 构建参数 |
| `6b4c0bc1` | `scripts/build-image-zip.sh` 镜像构建打包脚本（默认 goproxy.cn 镜像） |

---

## 维护提示

1. **merge dev 迁移风险面**：dev 大重构（如 0.4.0 编排层拆分）后，定制功能链路（fan-out/讨论/禁用/删除）需要重新验证接线——`1dfd15ca` 修复的 override 断链即 `f306ece6` 迁移的遗留损伤。每次合并 dev 后建议冒烟：多模型并行各 tab 内容确属不同模型、讨论多轮正常、禁用/删除链路可用。
2. **冒烟路径**：管理后台配 ≥2 模型渠道 → 聊天页多选组合发送 → 验证 tab/禁用/删除/讨论；聊天室双设备（或双浏览器）互发验证设备分边与实时同步；管理后台模型页验证孤儿清理徽标随本页变化、上游对话框批量测试（同前缀模型串行、结果列表点击切换）。
3. **仓库卫生**：`issue.md`、`todo_websearch.md` 为已跟踪的临时工作文档（审查报告/调研笔记），gitignore 目前仅忽略 `/todo.md`（`b48527e2`）；如需对外公开分支或向上游 PR，建议将其移出跟踪并补充 gitignore 条目。
